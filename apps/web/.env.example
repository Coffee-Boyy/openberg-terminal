# OpenBerg Terminal — Web Environment
# Copy to .env and fill in your values.

# Backend API URL (default: relative /api route)
VITE_API_URL=http://localhost:8000/api

# Use demo data when true (bypasses the backend for offline demos)
VITE_DEMO_MODE=false
